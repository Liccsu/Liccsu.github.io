import{_ as e,o as s,c as a,e as n}from"./app-C4bsKav7.js";const i={},d=n(`<h1 id="配置github-ssh默认使用443端口" tabindex="-1"><a class="header-anchor" href="#配置github-ssh默认使用443端口" aria-hidden="true">#</a> 配置GitHub SSH默认使用443端口</h1><h2 id="_1-测试443端口是否可用" tabindex="-1"><a class="header-anchor" href="#_1-测试443端口是否可用" aria-hidden="true">#</a> 1.测试443端口是否可用</h2><p>首先测试一下使用443端口进行GitHub SSH连接：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ssh</span> <span class="token parameter variable">-T</span> <span class="token parameter variable">-p</span> <span class="token number">443</span> git@ssh.github.com
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果一切正常，那么你应该得到如下输出：</p><blockquote><p>Hi UserName! You&#39;ve successfully authenticated, but GitHub does not provide shell access.</p></blockquote><h2 id="_2-配置ssh使用443端口" tabindex="-1"><a class="header-anchor" href="#_2-配置ssh使用443端口" aria-hidden="true">#</a> 2.配置SSH使用443端口</h2><p><strong>SSH客户端配置文件</strong></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># 全局配置文件：/etc/ssh/ssh_config</span>
<span class="token comment"># 用户配置文件：~/.ssh/config # 如果不存在则创建</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>在你的SSH客户端配置文件中增加以下内容：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>Host github.com
  Hostname ssh.github.com
  Port 443
  User git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>再次测试是否有效：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ssh</span> <span class="token parameter variable">-T</span> git@github.com
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>或拉取仓库：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> clone git@github.com:UserName/RepoName.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,15),t=[d];function c(r,l){return s(),a("div",null,t)}const u=e(i,[["render",c],["__file","20240320-peizhiGitHub-SSHmorenshiyong443duankou.html.vue"]]);export{u as default};
